# R2 Sync Configuration - Local Development

# Your Cloudflare account ID
# Find this at: https://dash.cloudflare.com/?to=/:account/overview
CLOUDFLARE_ACCOUNT_ID=

# R2 API Token credentials
# Create at: https://dash.cloudflare.com/?to=/:account/r2/tokens
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# Usage:
# -----------------------------------------
# Environment is required when syncing:
#   pnpm sync:r2 --env staging      # Syncs to portfolio-documents-staging
#   pnpm sync:r2 --env production   # Syncs to portfolio-documents-prod
#
# R2 buckets are per-environment due to 1:1:1 mapping (R2 → Queue → Worker).
# Local development uses wrangler's local R2 storage (no sync needed).
